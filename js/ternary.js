const username = `darshan is ${true ? 'male' : 'female'} student.`

const gender = 'M'

const user = `${gender.toLocaleLowerCase() === 'm' ? 'He' : 'She'} is a college student.`

console.log(user)

const result = 0 ? 'Darshan' : 'Ajudiya' ? 'BVM' : 'clg'

console.log(result)